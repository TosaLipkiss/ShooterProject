void startButton()
{
  noStroke();
  fill(250, 150, 10);
  rectMode(CENTER);
  rect(width / 2, height / 2 + 100, 300, 100);

  fill(0, 0, 0);
  textSize(42);
  textAlign(CENTER, CENTER);
  text("START", width / 2, height / 2 + 100);
}
